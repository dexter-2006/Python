# functions is the block of code which only runs when we call
# it is used to make code short reusable

# to create function in python we use def keyword
def myfunction():
    print("hello function")

myfunction() # calling of function

# just remeber function name not start with number

def conert(f):
    return (f-32)*(5/9)
print(conert(77))
print(f"degree is := {conert(90)}")

# to send data back to funtion use return() keyword

# diffrence betweem parameter and argument
 # parameter := myfun(parameter) 
 # myfun(value) argument

 # we have do function in details later

